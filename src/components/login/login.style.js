import styled from "@emotion/styled";

const LoginContainer = styled.div`
  .loginButton {
    float: left;
    margin-left: 5%; 
  }
  .form-group label {
    float: left;
  }
`;

export default LoginContainer;
